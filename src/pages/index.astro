---
import Layout from '../layouts/Layout.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
import ServiciosGrid from '../components/ServiciosGrid.astro';
import { getServicios } from '../lib/wordpress.js';

const serviciosHome = (await getServicios()).slice(0, 4);
---


<Layout title="Elqui Capital | Gestión Técnica Inmobiliaria">
	<Header />
	
	<section id="inicio" class="hero">
		<div class="hero-background">
			<div class="gradient-orb orb-1"></div>
			<div class="gradient-orb orb-2"></div>
			<div class="gradient-orb orb-3"></div>
		</div>
		
		<div class="hero-content">
			<h1>
				Gestión Técnica Inmobiliaria<br>
				<span class="highlight">para una operación continua</span>
			</h1>
			<p class="subtitle">Permisos municipales, inspección técnica y mantención especializada para proyectos comerciales, corporativos y residenciales.</p>
			<div class="cta-group">
				<a href="/contacto" class="cta-button primary">
					Contáctanos
					<svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
						<path d="M5 12h14M12 5l7 7-7 7"/>
					</svg>
				</a>
				<a href="/servicios" class="cta-button secondary">Ver Servicios</a>
			</div>
		</div>
	</section>

	<section class="casos-section">
		<div class="container">
			<div class="section-header">
				<h2>Servicios</h2>
				<p class="section-subtitle">Permisos | Inspección | Control | Mantención Eléctrica</p>
			</div>

			<div class="casos-grid">
				{serviciosHome && serviciosHome.length > 0 ? (
					serviciosHome.map((servicio: any) => (
						<a class="caso-card" href={`/servicios/${servicio.slug}`} aria-label={servicio.title?.rendered || 'Servicio'}>
							{servicio._embedded?.['wp:featuredmedia']?.[0]?.source_url ? (
								<div class="caso-media">
									<img
										src={servicio._embedded['wp:featuredmedia'][0].source_url}
										alt={servicio.title?.rendered || 'Servicio'}
										loading="lazy"
									/>
								</div>
							) : (
								<div class="caso-media" aria-hidden="true" />
							)}
							<div class="caso-body">
								<h3 class="caso-title" set:html={servicio.title?.rendered} />
								<span class="caso-link">Ver detalle</span>
							</div>
						</a>
					))
				) : (
					<p class="no-servicios">No hay servicios disponibles</p>
				)}
			</div>

			<div class="casos-cta">
				<a href="/servicios" class="cta-button primary">Ver detalle de servicios</a>
			</div>
		</div>
	</section>

	<section id="nosotros" class="nosotros-section">
		<div class="container">
			<div class="section-header">
				<span class="section-badge">Nosotros</span>
				<h2>Gestión técnica inmobiliaria para operar sin contingencias</h2>
			</div>
			
			<div class="nosotros-content">
				<div class="nosotros-image">
					<img src="/images/foto-abogado.png" alt="Elqui Capital" />
				</div>
				
				<div class="nosotros-text">
					<p>Elqui Capital es una oficina de gestión técnica inmobiliaria especializada en proyectos comerciales, corporativos y residenciales a nivel nacional.</p>
					<p>Nuestro foco es gestionar técnicamente proyectos para que operen sin contingencias municipales, sanitarias o técnicas.</p>
					<p>Actuamos como respaldo técnico del mandante frente a municipios, servicios sanitarios y procesos de inspección.</p>
				</div>
			</div>
		</div>
	</section>

	<section class="porque-section">
		<div class="container">
			<div class="section-header">
				<h2>Experiencia y cobertura nacional</h2>
			</div>

			<div class="porque-grid">
				<div class="porque-item">
					<div class="porque-icon">
						<svg width="32" height="32" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
							<path d="M20 10c0 7-8 13-8 13S4 17 4 10a8 8 0 0 1 16 0Z"/>
							<circle cx="12" cy="10" r="3"/>
						</svg>
					</div>
					<h3>Cobertura nacional</h3>
				</div>

				<div class="porque-item">
					<div class="porque-icon">
						<svg width="32" height="32" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
							<path d="M3 7h18"/>
							<path d="M3 12h18"/>
							<path d="M3 17h18"/>
						</svg>
					</div>
					<h3>Marcas atendidas</h3>
				</div>

				<div class="porque-item">
					<div class="porque-icon">
						<svg width="32" height="32" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
							<path d="M20 7h-9"/>
							<path d="M14 17H5"/>
							<circle cx="17" cy="17" r="3"/>
						</svg>
					</div>
					<h3>Continuidad operativa</h3>
				</div>

				<div class="porque-item">
					<div class="porque-icon">
						<svg width="32" height="32" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
							<path d="M12 20h9"/>
							<path d="M16.5 3.5a2.121 2.121 0 0 1 3 3L7 19l-4 1 1-4Z"/>
						</svg>
					</div>
					<h3>Respaldo técnico del mandante</h3>
				</div>
			</div>

			<div class="casos-cta">
				<a href="/experiencia" class="cta-button secondary">Ver experiencia</a>
			</div>
		</div>
	</section>



	<section id="consulta" class="consulta-section">
		<div class="container">
			<div class="section-header">
				<h2>Evaluación técnica inicial</h2>
			</div>

			<div class="consulta-grid">
				<div class="consulta-item">
					<div class="consulta-icon">
						<svg width="32" height="32" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
							<path d="M12 20h9"/>
							<path d="M16.5 3.5a2.121 2.121 0 0 1 3 3L7 19l-4 1 1-4Z"/>
						</svg>
					</div>
					<h3>Identificamos brechas normativas y riesgos</h3>
				</div>

				<div class="consulta-item">
					<div class="consulta-icon">
						<svg width="32" height="32" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
							<path d="M3 3v18h18"/>
							<path d="M7 14l4-4 4 4 6-6"/>
						</svg>
					</div>
					<h3>Definimos oportunidades de mejora</h3>
				</div>

				<div class="consulta-item">
					<div class="consulta-icon">
						<svg width="32" height="32" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
							<path d="M3 12h18"/>
							<path d="M12 3v18"/>
						</svg>
					</div>
					<h3>Proponemos una estrategia clara de gestión y seguimiento</h3>
				</div>

			<div class="consulta-beneficios">
				<div class="beneficio-badge">
					<svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
						<polyline points="20 6 9 17 4 12"/>
					</svg>
					<span>Gestión y seguimiento</span>
				</div>
				<div class="beneficio-badge">
					<svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
						<polyline points="20 6 9 17 4 12"/>
					</svg>
					<span>Continuidad operativa</span>
				</div>
				<div class="beneficio-badge">
					<svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
						<polyline points="20 6 9 17 4 12"/>
					</svg>
					<span>Cumplimiento normativo</span>
				</div>
			</div>

			<div class="consulta-cta">
				<a href="/contacto" class="cta-button primary">Solicitar contacto</a>
			</div>
		</div>
	</section>

	<Footer />
</Layout>

<style>
	.hero {
		min-height: 100vh;
		display: flex;
		align-items: center;
		justify-content: center;
		padding: 2rem;
		padding-top: 6rem;
		position: relative;
		overflow: hidden;
		background: linear-gradient(135deg, rgba(var(--eq-navy-rgb), 0.82) 0%, rgba(var(--eq-blue-rgb), 0.72) 100%), url('/images/inmobiliaria-elquicapital.jpg');
		background-size: cover;
		background-position: center;
		background-attachment: fixed;
		color: #ffffff;
	}

	.hero-background {
		position: absolute;
		inset: 0;
		overflow: hidden;
	}

	.gradient-orb {
		position: absolute;
		border-radius: 50%;
		filter: blur(100px);
		opacity: 0.4;
		animation: float 20s ease-in-out infinite;
	}

	.orb-1 {
		width: 600px;
		height: 600px;
		background: linear-gradient(135deg, #a5b4fc 0%, #c4b5fd 100%);
		top: -15%;
		left: -15%;
		animation-delay: 0s;
	}

	.orb-2 {
		width: 500px;
		height: 500px;
		background: linear-gradient(135deg, #fbbf24 0%, #f59e0b 100%);
		bottom: -15%;
		right: -15%;
		animation-delay: 7s;
	}

	.orb-3 {
		width: 450px;
		height: 450px;
		background: linear-gradient(135deg, #60a5fa 0%, #3b82f6 100%);
		top: 50%;
		left: 50%;
		transform: translate(-50%, -50%);
		animation-delay: 14s;
	}

	@keyframes float {
		0%, 100% { transform: translate(0, 0) scale(1); }
		33% { transform: translate(30px, -30px) scale(1.1); }
		66% { transform: translate(-20px, 20px) scale(0.9); }
	}

	.hero-content {
		position: relative;
		z-index: 1;
		max-width: 900px;
		text-align: center;
		color: #ffffff;
	}

	.hero-content h1 {
		color: #ffffff;
		text-shadow: 0 2px 10px rgba(0, 0, 0, 0.3);
	}

	.badge {
		display: inline-block;
		padding: 0.75rem 1.5rem;
		background: rgba(255, 255, 255, 0.15);
		backdrop-filter: blur(10px);
		border: 1px solid rgba(255, 255, 255, 0.3);
		border-radius: 50px;
		font-size: 0.875rem;
		font-weight: 600;
		letter-spacing: 0.5px;
		margin-bottom: 2rem;
		text-transform: uppercase;
		color: #ffffff;
		box-shadow: 0 4px 15px rgba(0, 0, 0, 0.2);
	}

	h1 {
		font-size: 72px;
		font-weight: 800;
		margin-bottom: 1.5rem;
		line-height: 1.2;
		letter-spacing: -0.02em;
		font-family: 'Inter', sans-serif !important;
	}

	/* Tablet */
	@media (max-width: 1024px) {
		h1 {
			font-size: 48px;
		}
	}

	/* Mobile */
	@media (max-width: 768px) {
		h1 {
			font-size: 32px;
			line-height: 1.3;
		}
	}

	h2 {
		font-size: 32px;
		color: var(--eq-text);
		font-weight: 700;
	}

	.highlight {
		color: var(--eq-accent);
		text-shadow: 0 2px 10px rgba(0, 0, 0, 0.3);
	}

	.subtitle {
		font-size: clamp(1rem, 3vw, 1.25rem);
		line-height: 1.6;
		color: rgba(255, 255, 255, 0.95);
		margin-bottom: 3rem;
		max-width: 700px;
		margin-left: auto;
		margin-right: auto;
		text-shadow: 0 2px 8px rgba(0, 0, 0, 0.3);
	}

	.cta-group {
		display: flex;
		gap: 1rem;
		justify-content: center;
		flex-wrap: wrap;
		margin-bottom: 4rem;
	}

	.cta-button {
		display: inline-flex;
		align-items: center;
		gap: 0.5rem;
		padding: 1rem 2rem;
		text-decoration: none;
		border-radius: 12px;
		font-weight: 600;
		font-size: 1rem;
		transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
		border: 2px solid transparent;
	}

	.cta-button.primary {
		background: var(--eq-primary-dark);
		color: white;
		box-shadow: 0 10px 30px rgba(var(--eq-blue-rgb), 0.3);
	}

	.cta-button.primary:hover {
		transform: translateY(-2px);
		box-shadow: 0 15px 40px rgba(var(--eq-blue-rgb), 0.4);
	}

	.cta-button.secondary {
		background: rgba(255, 255, 255, 0.9);
		backdrop-filter: blur(10px);
		border: 2px solid var(--eq-primary);
		color: var(--eq-primary);
	}

	.cta-button.secondary:hover {
		background: var(--eq-primary);
		color: white;
		border-color: var(--eq-primary);
	}

	.stats {
		display: grid;
		grid-template-columns: repeat(auto-fit, minmax(150px, 1fr));
		gap: 2rem;
		max-width: 600px;
		margin: 0 auto;
		padding: 2rem;
		background: rgba(255, 255, 255, 0.05);
		backdrop-filter: blur(10px);
		border: 1px solid rgba(255, 255, 255, 0.1);
		border-radius: 20px;
	}

	.stat-item {
		text-align: center;
	}

	.stat-number {
		font-size: 2.5rem;
		font-weight: 800;
		background: linear-gradient(135deg, var(--eq-primary-dark) 0%, var(--eq-accent) 100%);
		-webkit-background-clip: text;
		-webkit-text-fill-color: transparent;
		background-clip: text;
		margin-bottom: 0.5rem;
	}

	.stat-label {
		font-size: 0.875rem;
		color: rgba(255, 255, 255, 0.7);
		font-weight: 500;
	}

	.hero-stats {
		display: flex;
		justify-content: center;
		gap: 3rem;
		margin-top: 4rem;
	}

	.stat-item {
		text-align: center;
		padding: 1.5rem 2rem;
		background: rgba(255, 255, 255, 0.8);
		backdrop-filter: blur(10px);
		border-radius: 16px;
		border: 1px solid rgba(var(--eq-blue-rgb), 0.18);
		box-shadow: 0 4px 15px rgba(0, 0, 0, 0.05);
	}

	.stat-number {
		font-size: 2.5rem;
		font-weight: 800;
		background: linear-gradient(135deg, var(--eq-primary-dark) 0%, var(--eq-accent) 100%);
		-webkit-background-clip: text;
		-webkit-text-fill-color: transparent;
		background-clip: text;
		line-height: 1;
		margin-bottom: 0.5rem;
	}

	.stat-label {
		font-size: 0.875rem;
		color: var(--eq-text);
		font-weight: 600;
		text-transform: uppercase;
		letter-spacing: 0.5px;
	}

	@media (max-width: 768px) {
		.hero {
			min-height: 100svh;
			padding: 1.5rem;
		}

		.stats {
			grid-template-columns: 1fr;
			gap: 1.5rem;
			padding: 1.5rem;
		}

		.cta-group {
			flex-direction: column;
			align-items: stretch;
		}

		.cta-button {
			justify-content: center;
		}
	}

	/* Sección Nosotros */
	.nosotros-section {
		padding: 6rem 2rem;
		background: linear-gradient(180deg, #ffffff 0%, #f8fafc 100%);
	}

	.nosotros-section .container {
		max-width: 1200px;
		margin: 0 auto;
	}

	.nosotros-section .section-header {
		text-align: center;
		margin-bottom: 4rem;
	}

	.nosotros-section .section-badge {
		display: inline-block;
		padding: 0.5rem 1.5rem;
		background: linear-gradient(135deg, var(--eq-primary-dark) 0%, var(--eq-accent) 100%);
		color: white;
		border-radius: 50px;
		font-size: 0.875rem;
		font-weight: 600;
		letter-spacing: 0.5px;
		margin-bottom: 1rem;
		text-transform: uppercase;
	}

	.nosotros-section h2 {
		font-size: 32px;
		margin-bottom: 1rem;
		color: var(--eq-text);
		font-weight: 700;
		letter-spacing: -0.02em;
	}

	.nosotros-section .section-subtitle {
		font-size: 1.125rem;
		color: var(--eq-text-muted);
		max-width: 700px;
		margin: 0 auto;
	}

	.nosotros-content {
		display: grid;
		grid-template-columns: 1fr 1fr;
		gap: 4rem;
		align-items: start;
	}

	.nosotros-image {
		position: relative;
		border-radius: 20px;
		overflow: hidden;
		box-shadow: 0 20px 60px rgba(0, 0, 0, 0.15);
	}

	.nosotros-image img {
		width: 100%;
		height: auto;
		display: block;
		object-fit: cover;
	}

	.nosotros-text p {
		color: var(--eq-text-muted);
		line-height: 1.8;
		margin-bottom: 1.5rem;
		font-size: 1.05rem;
	}

	.nosotros-text strong {
		color: var(--eq-text);
		font-weight: 700;
	}

	@media (max-width: 768px) {
		.nosotros-section {
			padding: 4rem 1.5rem;
		}

		.nosotros-content {
			grid-template-columns: 1fr;
			gap: 2rem;
		}
	}

	/* Sección Casos */
	.casos-section {
		padding: 6rem 2rem;
		background: var(--eq-surface);
	}

	.casos-section .container {
		max-width: 1400px;
		margin: 0 auto;
	}

	.casos-section .section-subtitle {
		font-size: 20px;
		line-height: 17px;
		color: var(--eq-primary);
	}

	.casos-grid {
		display: grid;
		grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
		gap: 1.5rem;
		margin-top: 3rem;
	}

	.caso-card {
		background: white;
		padding: 0;
		border-radius: 10px;
		border: 1px solid #e2e8f0;
		transition: all 0.3s ease;
		text-align: left;
		overflow: hidden;
	}

	.caso-card:hover {
		transform: translateY(-5px);
		box-shadow: 0 10px 30px rgba(30, 58, 138, 0.15);
		border-color: var(--eq-primary);
	}

	.caso-media {
		width: 100%;
		height: 168px;
		background: rgba(var(--eq-blue-rgb), 0.08);
		border-bottom: 1px solid rgba(var(--eq-blue-rgb), 0.12);
		overflow: hidden;
	}

	.caso-media img {
		width: 100%;
		height: 100%;
		object-fit: cover;
		display: block;
		transition: transform 0.35s ease;
	}

	.caso-card:hover .caso-media img {
		transform: scale(1.04);
	}

	.caso-body {
		padding: 1.5rem 1.5rem 1.25rem;
		display: flex;
		flex-direction: column;
		gap: 0.75rem;
	}

	.caso-title {
		margin: 0;
		color: var(--eq-text);
		font-weight: 750;
		font-size: 1.05rem;
		line-height: 1.35;
		letter-spacing: -0.01em;
	}

	.caso-link {
		color: var(--eq-primary);
		font-weight: 600;
		font-size: 0.95rem;
	}

	.caso-card {
		display: block;
		text-decoration: none;
	}

	.casos-cta {
		text-align: center;
		margin-top: 3rem;
	}

	/* Sección Por qué */
	.porque-section {
		padding: 6rem 2rem;
		background: linear-gradient(135deg, var(--eq-surface) 0%, rgba(var(--eq-sky-rgb), 0.10) 100%);
	}

	.porque-grid {
		display: grid;
		grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
		gap: 2rem;
		margin-top: 3rem;
	}

	.porque-item {
		background: white;
		padding: 2rem;
		border-radius: 16px;
		border: 1px solid rgba(var(--eq-blue-rgb), 0.18);
		transition: all 0.3s ease;
		text-align: center;
	}

	.porque-item:hover {
		transform: translateY(-5px);
		box-shadow: 0 10px 30px rgba(30, 58, 138, 0.15);
		border-color: var(--eq-primary);
	}

	.porque-icon {
		width: 64px;
		height: 64px;
		margin: 0 auto 1.5rem;
		display: flex;
		align-items: center;
		justify-content: center;
		background: var(--eq-primary-dark);
		border-radius: 12px;
		color: white;
	}

	.porque-item h3 {
		color: var(--eq-text);
		font-weight: 700;
		font-size: 1.1rem;
		line-height: 1.5;
	}

	/* Sección Consulta */
	.consulta-section {
		padding: 6rem 2rem;
		background: var(--eq-primary-dark);
		color: white;
	}
	.consulta-section h2 {
		color: var(--eq-text-inverse);
	}

	.consulta-grid {
		display: grid;
		grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
		gap: 2rem;
		margin-top: 3rem;
	}

	.consulta-item {
		background: rgba(255, 255, 255, 0.05);
		backdrop-filter: blur(10px);
		border: 1px solid rgba(255, 255, 255, 0.1);
		border-radius: 16px;
		padding: 2rem;
		text-align: center;
		transition: all 0.3s ease;
	}

	.consulta-item:hover {
		background: rgba(255, 255, 255, 0.08);
		border-color: rgba(var(--eq-sky-rgb), 0.45);
		transform: translateY(-5px);
	}

	.consulta-icon {
		width: 64px;
		height: 64px;
		margin: 0 auto 1.5rem;
		display: flex;
		align-items: center;
		justify-content: center;
		background: var(--eq-primary-dark);
		border-radius: 12px;
		color: white;
	}

	.consulta-item h3 {
		color: white;
		font-size: 1.1rem;
		font-weight: 600;
		line-height: 1.5;
	}

	.consulta-beneficios {
		display: flex;
		justify-content: center;
		gap: 2rem;
		margin-top: 3rem;
		flex-wrap: wrap;
	}

	.beneficio-badge {
		display: flex;
		align-items: center;
		gap: 0.5rem;
		padding: 0.75rem 1.5rem;
		background: rgba(var(--eq-sky-rgb), 0.16);
		border: 1px solid rgba(var(--eq-sky-rgb), 0.28);
		border-radius: 50px;
		color: var(--eq-text-inverse);
		font-weight: 600;
		font-size: 0.95rem;
	}

	.beneficio-badge svg {
		color: var(--eq-accent);
	}

	.consulta-cta {
		text-align: center;
		margin-top: 3rem;
	}

	@media (max-width: 768px) {
		.casos-section,
		.porque-section,
		.consulta-section {
			padding: 4rem 1.5rem;
		}

		.casos-grid,
		.porque-grid,
		.consulta-grid {
			grid-template-columns: 1fr;
			gap: 1.5rem;
		}

		.consulta-beneficios {
			flex-direction: column;
			align-items: center;
		}
	}
</style>
