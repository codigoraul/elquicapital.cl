---
import Layout from '../layouts/Layout.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
import TitleBreadcrumb from '../components/TitleBreadcrumb.astro';
---

<Layout
	title="Quiénes Somos | Elqui Capital"
	description="Conoce Elqui Capital: respaldo técnico para proyectos inmobiliarios. Gestión de permisos, inspección técnica y mantención para operar sin contingencias."
	canonicalPath="/quienes-somos"
>
	<Header />

	<main class="page">
		<TitleBreadcrumb
			title="Quiénes Somos"
			backgroundImage="/images/bg-title.jpg"
			breadcrumbs={[
				{ label: 'Inicio', href: '/' },
				{ label: 'Quiénes Somos' },
			]}
		/>

		<section class="content">
			<div class="container">
				<div class="about-grid">
					<div class="about-media">
						<img src="/images/nosotros-bg.jpg" alt="Elqui Capital" loading="lazy" />
					</div>
					<div class="about-text">
						<h2>Respaldo técnico para operar sin contingencias</h2>
						<p>Nuestro foco es gestionar técnicamente proyectos para que operen sin contingencias municipales, sanitarias o técnicas.</p>
						<p>Actuamos como respaldo técnico del mandante frente a municipios, servicios sanitarios y procesos de inspección.</p>
					</div>
				</div>
			</div>
		</section>
	</main>

	<Footer />
</Layout>

<style>
	.page {
		padding-top: 7rem;
	}

	.container {
		max-width: 1200px;
		margin: 0 auto;
		padding: 0 2rem;
	}

	.content {
		padding: 4rem 0 5rem;
	}

	.content > .container {
		padding-top: 20px;
		padding-bottom: 20px;
	}

	.about-grid {
		display: grid;
		grid-template-columns: 1fr 1fr;
		gap: 2.5rem;
		align-items: center;
	}

	.about-text {
		padding: 0.25rem 0;
	}

	.about-text h2 {
		margin: 0 0 1rem;
		color: var(--eq-text);
		font-size: 32px;
		line-height: 1.2;
		letter-spacing: -0.02em;
		font-weight: 700;
	}

	.about-text p {
		margin: 0 0 1rem;
		color: var(--eq-text-muted);
		line-height: 1.8;
		font-size: 1.05rem;
	}

	.about-text p:last-child {
		margin-bottom: 0;
	}

	.about-media {
		border-radius: 16px;
		overflow: hidden;
		border: 1px solid rgba(var(--eq-blue-rgb), 0.18);
		box-shadow: 0 18px 50px rgba(0, 0, 0, 0.12);
		background: rgba(255, 255, 255, 0.75);
	}

	.about-media img {
		width: 100%;
		height: 100%;
		max-height: 520px;
		object-fit: cover;
		display: block;
	}

	@media (max-width: 900px) {
		.about-grid {
			grid-template-columns: 1fr;
		}

		.about-media {
			order: -1;
		}

		.about-media img {
			max-height: 360px;
		}
	}
</style>
